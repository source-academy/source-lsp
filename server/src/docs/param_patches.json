{
    "accumulate": [
        "${1:(x, y) => }",
        "${2:initial}",
        "${3:xs}"
    ],
    "build_list": [
        "${1:x => }",
        "${2:n}"
    ],
    "build_stream": [
        "${1:x => }",
        "${2:n}"
    ],
    "filter": [
        "${1:x => }",
        "${2:xs}"
    ],
    "for_each": [
        "${1:x => }",
        "${2:xs}"
    ],
    "map": [
        "${1:x => }",
        "${2:xs}"
    ],
    "stream_filter": [
        "${1:x => }",
        "${2:xs}"
    ],
    "stream_for_each": [
        "${1:x => }",
        "${2:xs}"
    ],
    "stream_map": [
        "${1:x => }",
        "${2:xs}"
    ]
}